services:
  - type: web
    name: flask-poetry
    env: python
    plan: starter
    branch: robdimarco/with-build-script
    buildCommand: ./build.sh
    startCommand: gunicorn app:app --chdir ./poetry_flask
    autoDeploy: false
    envVars:
    - key: PYTHON_VERSION
      value: 3.11.8
